name: "test-job"
on:
  pull_request:
    branches:
        - main
    paths-ignore:
      - '**.md'
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'

jobs:
  test-job:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
    steps:
      - name: Action step
        id: actionId
        uses: ralvescosta/insiderci-action@v0.0.24
        with:
          email: 'rafael.costa@inmetrics.com.br'
          password: 'Trocar@123$2021'
          technology: 'typescript'
          security: 50

